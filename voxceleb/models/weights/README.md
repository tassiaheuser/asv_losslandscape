# Local Model Weights

Put pretrained checkpoints and locally trained model files under this directory.

Example configs reference paths such as:

```text
models/weights/ResNetSE34V2/baseline_v2_smproto.model
```

Checkpoint files and TensorBoard event logs are ignored by git because they are generated artifacts
and may have separate redistribution restrictions.
